% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hsic.R
\name{hsic_resid_test}
\alias{hsic_resid_test}
\title{HSIC Regression Residual Independence Test}
\usage{
hsic_resid_test(
  model,
  x = NULL,
  kernel_x = "gaussian",
  kernel_y = kernel_x,
  bandwidth_x = NULL,
  bandwidth_y = NULL,
  degree = 2L,
  coef0 = 1,
  B = 1000L
)
}
\arguments{
\item{model}{A fitted model (typically \code{\link[stats]{lm}}) with
working \code{model.matrix}, \code{coef}, \code{nobs}, \code{resid}.}

\item{x}{Numeric predictor matrix for the HSIC computation.  If
\code{NULL} (default), \code{model.matrix(model)} is used with the
intercept column removed.}

\item{kernel_x}{Kernel for \eqn{X}.  Default \code{"gaussian"}.}

\item{kernel_y}{Kernel for residuals.  Default \code{kernel_x}.}

\item{bandwidth_x}{Bandwidth (\eqn{\sigma^2}) for \eqn{X}.  \code{NULL}
uses the median heuristic.}

\item{bandwidth_y}{Bandwidth for residuals.  \code{NULL} uses the median
heuristic.}

\item{degree}{Polynomial degree.  Default \code{2}.}

\item{coef0}{Polynomial constant.  Default \code{1}.}

\item{B}{Bootstrap replications.  Default \code{1000}.}
}
\value{
An \code{"htest"} object with \code{statistic} = \eqn{n \times
  \widehat{\mathrm{HSIC}}(X, \hat{e})}, \code{p.value} from the
  Laplace-corrected bootstrap \eqn{(\#\{T_b \geq T_{\mathrm{obs}}\}+1)/(B+1)},
  and \code{bandwidths}.
}
\description{
Tests \eqn{H_0: X \perp\!\!\!\perp \hat{e}} where
  \eqn{\hat{e}} are OLS residuals from a fitted linear model.  Rejection
  indicates misspecification: a missing nonlinear term, omitted variable,
  or heteroscedasticity.

  The null distribution is estimated via the pairs-bootstrap of the
  \pkg{dHSIC} source (Peters et al., 2022).  The test statistic is
  \eqn{n \times \widehat{\mathrm{HSIC}}}, consistent with
  \code{\link{hsic.test}}.
}
\examples{
\dontrun{
set.seed(99); n <- 100; x <- rnorm(n)
hsic_resid_test(lm(2*x + rnorm(n) ~ x), B = 299)       # large p-value
hsic_resid_test(lm(x^2 + rnorm(n, sd=0.5) ~ x), B = 299) # small p-value
}

}
\references{
Peters, J., Pfister, N., & Mooij, J. M. (2022). \emph{dHSIC}. R package
  version 2.1.
}
\seealso{
\code{\link{hsic}}, \code{\link{hsic.test}}
}
\keyword{internal}
